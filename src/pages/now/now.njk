---
title: Now
menuactive: about
permalink: '/now/'
sitemap:
  priority: 0.6
---

<div class="content content--narrow">
  <h1 class="page-title" data-kanji="今">I am...</h1>
  <ul>
    <li>...in
      <a href="{{ now.location.citylink }}" target="_blank">{{ now.location.city }}</a>
      {% if now.location.doinglink %}
        <a href="{{ now.location.doinglink }}" target="_blank">{{ now.location.doing }}</a>
      {% else %}
        {{ now.location.doing }}
      {% endif %}
    </li>
    <li>...reading
      <a href="{{ now.reading.url }}" target="_blank">{{ now.reading.title }}</a>
      by {{ now.reading.author }}</li>
    <li>...most listened song
      <a href="{{ now.listening.url }}" target="_blank">{{ now.listening.name }}</a>
      by {{ now.listening.artist }}</li>
    <li>...binge watching
      <a href="{{ now.watching.url }}" target="_blank">{{ now.watching.title }}</a>
    </li>
    <li>...working for
      <a href="{{ now.working.url }}" target="_blank">{{ now.working.company }}</a>
    </li>
  </ul>
</div>