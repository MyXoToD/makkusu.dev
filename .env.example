# Copy to .env on the server, next to docker-compose.yml.
# Compose reads this file automatically, so `docker compose up -d` needs no
# `export` first. The deploy workflow passes TAG_NAME inline, which takes
# precedence over whatever is set here.
TAG_NAME=v1.2.14
